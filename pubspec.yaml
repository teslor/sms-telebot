name: sms_telebot
description: "App to automatically forward incoming SMS messages to a Telegram bot."
publish_to: 'none'
version: 0.3.0

environment:
  sdk: ^3.7.2

dependencies:
  baseflow_plugin_template: ^2.2.0
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.3.0
  intl: any
  permission_handler: ^12.0.0+1
  provider: ^6.1.5
  readsms: ^0.2.0+4
  shared_preferences: ^2.5.3
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true

# https://docs.flutter.dev/tools/pubspec